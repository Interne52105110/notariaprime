import Link from 'next/link';
import MainLayout from '@/components/MainLayout';
import { guides } from '@/content/guides';
import { buildMetadata } from '@/lib/seo';
export const metadata=buildMetadata('/guides');
export default function GuidesPage(){return <MainLayout><div className="mx-auto max-w-6xl px-5 py-12 text-slate-800"><header className="rounded-3xl bg-indigo-50 p-7 md:p-12"><p className="text-sm font-semibold uppercase tracking-wider text-indigo-700">La méthode derrière le résultat</p><h1 className="mt-4 text-3xl font-bold md:text-5xl">Préparer votre opération avec des exemples chiffrés</h1><p className="mt-5 max-w-3xl text-lg leading-relaxed">Comprenez les données à saisir, les pièces à réunir et les limites du calcul avant un achat, une transmission ou un investissement.</p><nav aria-label="Choisir son parcours" className="mt-6 flex flex-wrap gap-3"><a href="#particulier" className="rounded-lg bg-indigo-700 px-5 py-3 text-white">Je prépare mon opération</a><a href="#professionnel" className="rounded-lg border border-indigo-700 px-5 py-3 text-indigo-800">Je travaille sur un dossier</a></nav></header>{(['particulier','professionnel'] as const).map(a=><section id={a} key={a} className="scroll-mt-24 pt-10"><h2 className="text-2xl font-bold">{a==='particulier'?'Immobilier, famille et patrimoine':'Pratique professionnelle'}</h2><div className="mt-5 grid gap-5 md:grid-cols-2">{guides.filter(g=>g.audience===a).map((g,i)=><article key={g.slug} className="rounded-2xl border p-6 transition hover:border-indigo-400"><p className="text-sm font-medium text-indigo-700">Guide {i+1} · Exemple et pièces utiles</p><h3 className="mt-2 text-xl font-semibold"><Link href={'/guides/'+g.slug} className="hover:underline">{g.title}</Link></h3><p className="mt-3 leading-relaxed text-slate-600">{g.description}</p><Link href={'/guides/'+g.slug} className="mt-4 inline-flex min-h-11 items-center font-medium text-indigo-700 underline">Lire le guide</Link></article>)}</div></section>)}<p className="mt-10 text-sm text-slate-600">Édition NotariaPrime du 14 septembre 2026. Chaque guide distingue exemple et situation individuelle. <Link className="underline" href="/methodologie-fiscale">Consulter la méthode et les limites des vérifications.</Link></p></div></MainLayout>}
